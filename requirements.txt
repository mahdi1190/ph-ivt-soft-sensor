# Core
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
scipy>=1.11
scikit-learn>=1.3

# Filtering
filterpy>=1.4.5

# DAE solver — not pip-installable on a clean machine: assimulo needs a system
# SUNDIALS installation to build. Install both via conda-forge instead
# (used by environment.yml):
#   conda install -c conda-forge assimulo sundials
